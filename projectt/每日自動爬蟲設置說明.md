# 每日自動爬蟲設置說明

## 📋 自動化爬蟲系統說明

已經為您創建了三個檔案，可以實現每天自動執行爬蟲：

### 1️⃣ 檔案說明

- **daily_auto_crawler.py** - Python 自動爬蟲腳本
  - 每天抓取 5 則新聞
  - 根據日期輪流使用不同關鍵字（健康、政治、經濟、科技、社會）
  - 自動儲存到 reports 資料夾

- **run_daily_crawler.bat** - Windows 批次檔
  - 執行 Python 腳本
  - 記錄執行日誌

- **setup_daily_task.ps1** - PowerShell 設置腳本
  - 自動設置 Windows 工作排程器
  - 預設每天早上 8:00 執行

---

## 🚀 快速設置（三種方法）

### 方法 A：自動設置（推薦）

1. **以系統管理員身分開啟 PowerShell**
2. 執行以下指令：
   ```powershell
   cd "c:\Users\rin\Desktop\truthliesdetector\projectt"
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   .\setup_daily_task.ps1
   ```
3. 按照提示操作，完成！

---

### 方法 B：手動使用工作排程器

1. **開啟「工作排程器」**
   - 按 `Win + R`
   - 輸入 `taskschd.msc`
   - 按 Enter

2. **建立基本工作**
   - 右側點選「建立基本工作」
   - 名稱：`TruthLiesDetector_DailyCrawler`
   - 描述：`每天自動執行爬蟲抓取新聞`

3. **設定觸發程序**
   - 選擇「每天」
   - 時間：`08:00:00`（或您想要的時間）
   - 勾選「啟用」

4. **設定動作**
   - 選擇「啟動程式」
   - 程式或指令碼：瀏覽選擇 `run_daily_crawler.bat`
   - 位置：`c:\Users\rin\Desktop\truthliesdetector\projectt`
   - 起始於：`c:\Users\rin\Desktop\truthliesdetector\projectt`

5. **完成設定**
   - 勾選「當按一下完成時，開啟此工作的內容對話方塊」
   - 在「條件」標籤中：
     - ✅ 只有在電腦連接網路時才啟動
     - ✅ 即使使用電池電源也執行
   - 按「確定」完成

---

### 方法 C：手動執行測試

如果不需要自動化，也可以隨時手動執行：

```powershell
cd "c:\Users\rin\Desktop\truthliesdetector\projectt"
python daily_auto_crawler.py
```

或直接雙擊執行：
```
run_daily_crawler.bat
```

---

## 🔧 管理指令

設置完成後，可以使用以下 PowerShell 指令管理任務：

```powershell
# 查看任務狀態
Get-ScheduledTask -TaskName "TruthLiesDetector_DailyCrawler"

# 手動立即執行
Start-ScheduledTask -TaskName "TruthLiesDetector_DailyCrawler"

# 停用任務
Disable-ScheduledTask -TaskName "TruthLiesDetector_DailyCrawler"

# 啟用任務
Enable-ScheduledTask -TaskName "TruthLiesDetector_DailyCrawler"

# 移除任務
Unregister-ScheduledTask -TaskName "TruthLiesDetector_DailyCrawler" -Confirm:$false
```

---

## 📊 查看執行結果

執行日誌會儲存在：
```
c:\Users\rin\Desktop\truthliesdetector\projectt\crawler_log.txt
```

爬取的資料會儲存在：
```
c:\Users\rin\Desktop\truthliesdetector\projectt\reports\
```

檔名格式：`raw_YYYYMMDD_HHMMSS.json`

---

## 🎯 自訂設定

### 修改執行時間
編輯工作排程器中的觸發程序，或重新執行 `setup_daily_task.ps1`

### 修改爬取關鍵字
編輯 `daily_auto_crawler.py` 第 18 行：
```python
keywords = ["健康", "政治", "經濟", "科技", "社會"]  # 自訂關鍵字清單
```

### 修改爬取數量
編輯 `daily_auto_crawler.py` 第 27 行：
```python
"--max-results", "5",  # 改成您想要的數量
```

---

## ✅ 驗證設定

設置完成後，可以立即測試：

```powershell
Start-ScheduledTask -TaskName "TruthLiesDetector_DailyCrawler"
```

然後檢查：
1. 是否產生新的 JSON 檔案
2. 日誌檔案是否有記錄
3. 前端頁面是否有新資料

---

## 💡 注意事項

1. **確保電腦有網路連線**時執行
2. **Python 環境**必須正確安裝所有依賴
3. **定期檢查日誌**確保爬蟲正常運作
4. **Gemini API** 額度充足

---

## 🆘 疑難排解

### 問題：任務沒有執行
- 檢查工作排程器中任務的「上次執行時間」
- 查看「上次執行結果」欄位
- 檢查電腦在設定時間是否開機

### 問題：執行失敗
- 檢查 `crawler_log.txt` 查看錯誤訊息
- 確認 Python 環境變數正確
- 手動執行 `python daily_auto_crawler.py` 測試

### 問題：沒有新資料
- 檢查關鍵字是否能搜尋到結果
- 確認網路連線正常
- 檢查 Gemini API 額度

---

**設置完成後，系統會每天自動執行爬蟲，您的圖表就會持續有最新資料！** 🎉
