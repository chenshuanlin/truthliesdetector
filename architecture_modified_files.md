# Truth Lies Detector - 修改檔案架構圖

## 📐 系統架構概覽

```
┌─────────────────────────────────────────────────────────────────┐
│                        Truth Lies Detector                       │
│                    假訊息偵測系統 - 修改架構圖                      │
└─────────────────────────────────────────────────────────────────┘

                          ┌──────────────┐
                          │   使用者介面   │
                          │   (Flutter)   │
                          └───────┬──────┘
                                  │
                ┌─────────────────┴─────────────────┐
                │                                   │
        ┌───────▼────────┐                 ┌───────▼────────┐
        │   主導航頁面     │                 │   個人資料頁面   │
        │   main.dart    │                 │ profile_page   │
        │   [已修改]      │                 │  [已修改]       │
        └───────┬────────┘                 └────────────────┘
                │
                │ 替換「發現」為「AI報告」
                │
        ┌───────▼────────────────────────────────────────────┐
        │          AI報告與趨勢分析頁面                         │
        │          ai_report_page.dart                        │
        │          [核心修改檔案]                              │
        │  ┌──────────────────────────────────────────────┐  │
        │  │ ✓ 移除「來源樣本20則」顯示                      │  │
        │  │ ✓ 標題改為「近七天假訊息監測報告」              │  │
        │  │ ✓ 顯示圖表、統計數據、熱門主題                  │  │
        │  └──────────────────────────────────────────────┘  │
        └───────┬────────────────────────────────────────────┘
                │
                │ HTTP GET /api/fake-news-stats
                │
        ┌───────▼────────────────────────────────────────────┐
        │              API 服務層 (Flask)                      │
        │              python_service/                        │
        └────────────────────────────────────────────────────┘
                │
                ├─────┐
                │     │
        ┌───────▼─────▼──────┐        ┌─────────────────────┐
        │   routes_stats.py  │        │  routes_auth.py     │
        │   [核心修改檔案]     │        │  (用戶認證)         │
        │                    │        └─────────────────────┘
        │ ✓ AI辨識率動態計算  │
        │ ✓ 統計改為最近7天   │
        │ ✓ 圖表顯示7天數據   │
        └───────┬────────────┘
                │
                │ 調用
                │
        ┌───────▼──────────────────────┐
        │  verification_loader.py      │
        │  (數據載入模組)                │
        │  [依賴，未直接修改]             │
        │                              │
        │ • load_verification_data()   │
        │ • classify_item()            │
        │ • get_daily_distribution()   │
        └───────┬──────────────────────┘
                │
                │ 讀取 JSON 檔案
                │
        ┌───────▼──────────────────────┐
        │   projectt/reports/          │
        │   (數據儲存目錄)               │
        │                              │
        │ • raw_20251009_024010.json   │
        │ • raw_20251010_120000.json   │
        │ • ...                        │
        │ • raw_20251021_190805.json ✨│
        └───────▲──────────────────────┘
                │
                │ 生成 JSON 檔案
                │
        ┌───────┴──────────────────────┐
        │      爬蟲系統 (projectt/)      │
        └──────────────────────────────┘
                │
    ┌───────────┼───────────┬──────────────┐
    │           │           │              │
┌───▼────┐ ┌───▼────┐ ┌────▼─────┐ ┌──────▼──────┐
│scraper │ │ daily_ │ │  run_    │ │  setup_     │
│  .py   │ │ auto_  │ │ daily_   │ │  daily_     │
│        │ │crawler │ │crawler   │ │  task.ps1   │
│        │ │  .py   │ │  .bat    │ │             │
│        │ │ [新增]  │ │ [新增]    │ │  [新增]      │
└────────┘ └────────┘ └──────────┘ └─────────────┘
```

---

## 🗂️ 檔案修改分層架構

### 第 1 層：前端 UI 層 (Flutter / Dart)

```
lib/
├── main.dart ⚙️
│   └── [修改] 將 Index 1 從「發現」替換為 AiReportPage
│
├── screens/
│   ├── ai_report_page.dart 🎨 [核心修改]
│   │   ├── 移除「來源樣本20則」顯示
│   │   ├── 標題改為「近七天假訊息監測報告」
│   │   └── 整合 /api/fake-news-stats API
│   │
│   └── profile_page.dart ⚙️
│       └── [修改] 個人資料更新功能
│
├── services/
│   └── api_service.dart ⚙️
│       └── [修改] updateUser 回應檢查邏輯
│
└── providers/
    └── user_provider.dart
        └── [使用] 調用 api_service 進行資料更新
```

### 第 2 層：後端 API 層 (Python / Flask)

```
python_service/
├── app.py 🚀
│   └── Flask 主程式 (啟動服務)
│
├── routes_stats.py 🎨 [核心修改]
│   ├── fake_news_stats() API 端點
│   ├── ✓ AI辨識率：動態計算 (verified/total)*100
│   ├── ✓ 統計範圍：最近7天 (非本週)
│   ├── ✓ 圖表數據：顯示完整星期一到日
│   └── ✓ 主題分類：基於最近7天資料
│
├── routes_auth.py ⚙️
│   └── [修改] 用戶更新 API 回應格式
│
└── verification_loader.py 📦
    └── [依賴] 載入與分類新聞資料
```

### 第 3 層：數據處理層 (Python)

```
projectt/
├── scraper.py 🕷️
│   └── [執行] 爬取新聞並生成 JSON
│
├── daily_auto_crawler.py ✨ [新增]
│   └── 每日自動爬蟲腳本
│
├── run_daily_crawler.bat ✨ [新增]
│   └── Windows 批次檔執行器
│
├── setup_daily_task.ps1 ✨ [新增]
│   └── 工作排程器設定腳本
│
└── reports/ 📁
    ├── raw_20251009_024010.json
    ├── raw_20251010_120000.json
    ├── ...
    └── raw_20251021_190805.json ✨ (最新)
```

---

## 🔄 數據流向圖

```
┌─────────────┐
│   用戶操作    │
│ (點擊AI報告) │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────┐
│   ai_report_page.dart       │
│   • 渲染「近七天」標題        │
│   • 不顯示「來源樣本」        │
└──────┬──────────────────────┘
       │
       │ HTTP GET Request
       │
┌──────▼──────────────────────┐
│   API: /api/fake-news-stats │
│   (routes_stats.py)         │
└──────┬──────────────────────┘
       │
       ├─ 1. 載入資料
       │
┌──────▼────────────────────────┐
│  verification_loader.py       │
│  • load_verification_data()   │
└──────┬────────────────────────┘
       │
       ├─ 2. 讀取 JSON 檔案
       │
┌──────▼──────────────────────┐
│   projectt/reports/*.json   │
│   (10個檔案，22則新聞)        │
└──────┬──────────────────────┘
       │
       ├─ 3. 分類與統計
       │
┌──────▼──────────────────────────────┐
│  routes_stats.py                    │
│  • 篩選最近7天資料                   │
│  • 計算 AI 辨識率                    │
│  • 生成圖表數據 (7天)                │
│  • 分類熱門主題                      │
└──────┬──────────────────────────────┘
       │
       │ JSON Response
       │
┌──────▼──────────────────────┐
│   ai_report_page.dart       │
│   • 顯示圖表 (一~日)         │
│   • 顯示統計 (已查證/待查證)  │
│   • 顯示 AI 辨識率 100%      │
│   • 顯示熱門主題 (健康100%)  │
└─────────────────────────────┘
```

---

## 🎯 核心修改對照表

| 層級 | 檔案 | 修改內容 | 影響範圍 |
|-----|------|---------|---------|
| **前端 UI** | `lib/main.dart` | 替換導航頁面 | 應用程式入口 |
| **前端 UI** | `lib/screens/ai_report_page.dart` | 移除來源顯示、改標題 | 視覺呈現 |
| **前端 UI** | `lib/services/api_service.dart` | 修正回應檢查邏輯 | 個人資料更新 |
| **後端 API** | `python_service/routes_stats.py` | AI辨識率、統計範圍、圖表 | 核心業務邏輯 |
| **後端 API** | `python_service/routes_auth.py` | 回應格式調整 | 用戶認證 |
| **自動化** | `projectt/daily_auto_crawler.py` | 新增每日爬蟲 | 數據自動生成 |
| **自動化** | `projectt/run_daily_crawler.bat` | 批次執行檔 | Windows 整合 |
| **自動化** | `projectt/setup_daily_task.ps1` | 工作排程設定 | 系統自動化 |

---

## 📊 關鍵修改點詳解

### 1️⃣ AI 辨識率計算 (routes_stats.py)

**修改前:**
```python
ai_accuracy = 70  # 硬編碼固定值
```

**修改後:**
```python
if total_week == 0:
    ai_accuracy = 0
else:
    ai_accuracy = round((verified_week / total_week) * 100)
```

---

### 2️⃣ 統計時間範圍 (routes_stats.py)

**修改前:**
```python
# 本週一到今天
week_start_date = (now - timedelta(days=today_weekday)).date()
```

**修改後:**
```python
# 最近7天（包含今天）
seven_days_ago = now.date() - timedelta(days=6)
```

---

### 3️⃣ 圖表顯示邏輯 (routes_stats.py)

**修改後:**
```python
# 顯示完整7天，按星期一到日排列
for i in range(7):
    days_ago = 6 - i  # 從6天前到今天
    date_for_day = now.date() - timedelta(days=days_ago)
    weekday_index = date_for_day.weekday()
    
    verified = verified_daily.get(days_ago, 0)
    suspicious = unverified_daily.get(days_ago, 0)
```

---

### 4️⃣ UI 標題修改 (ai_report_page.dart)

**修改前:**
```dart
title: '本週假訊息監測報告',
```

**修改後:**
```dart
title: '近七天假訊息監測報告',
```

---

## 🔐 用戶系統架構（額外修改）

```
┌──────────────────────────────────────┐
│        用戶認證與資料管理流程           │
└──────────────────────────────────────┘

┌─────────────┐
│   用戶操作    │
│ (更新個人資料) │
└──────┬──────┘
       │
┌──────▼────────────────┐
│  profile_page.dart    │
│  • 編輯個人資料表單    │
└──────┬────────────────┘
       │
┌──────▼────────────────┐
│  user_provider.dart   │
│  • updateUserProfile()│
└──────┬────────────────┘
       │
┌──────▼────────────────┐
│  api_service.dart ⚙️  │
│  • updateUser()       │
│  ✓ 檢查 data['ok']     │
└──────┬────────────────┘
       │
       │ HTTP POST
       │
┌──────▼────────────────┐
│  routes_auth.py ⚙️    │
│  • /api/users/:id     │
│  ✓ 回應 {'ok': True}   │
└──────┬────────────────┘
       │
┌──────▼────────────────┐
│   PostgreSQL 數據庫   │
│   • users 表          │
└───────────────────────┘
```

---

## 🚀 自動化系統架構

```
┌────────────────────────────────────────┐
│       Windows 工作排程器               │
│   TruthLiesDetector_DailyCrawler       │
│   ⏰ 每天早上 8:00 執行                 │
└────────────┬───────────────────────────┘
             │
             ▼
    ┌────────────────┐
    │ run_daily_     │
    │ crawler.bat    │
    └────────┬───────┘
             │
             ▼
    ┌────────────────────┐
    │ daily_auto_        │
    │ crawler.py         │
    │                    │
    │ • 根據日期選關鍵字  │
    │ • 呼叫 scraper.py  │
    │ • 記錄日誌         │
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │ scraper.py         │
    │                    │
    │ • DuckDuckGo 搜尋  │
    │ • 內容擷取         │
    │ • Gemini AI 分析   │
    │ • LightGBM 評分    │
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │ reports/           │
    │ raw_YYYYMMDD_      │
    │ HHMMSS.json        │
    └────────────────────┘
```

---

## 📝 修改檔案清單 (依修改程度排序)

### 🔴 核心修改（重大變更）
1. `python_service/routes_stats.py` - 業務邏輯核心
2. `lib/screens/ai_report_page.dart` - UI 顯示核心

### 🟡 中度修改（功能調整）
3. `lib/main.dart` - 導航結構調整
4. `lib/services/api_service.dart` - API 回應處理
5. `python_service/routes_auth.py` - 認證回應格式

### 🟢 新增檔案（擴充功能）
6. `projectt/daily_auto_crawler.py` ✨
7. `projectt/run_daily_crawler.bat` ✨
8. `projectt/setup_daily_task.ps1` ✨
9. `projectt/每日自動爬蟲設置說明.md` ✨

### ⚪ 依賴檔案（未直接修改但相關）
10. `python_service/verification_loader.py`
11. `projectt/scraper.py`
12. `lib/providers/user_provider.dart`
13. `lib/screens/profile_page.dart`

---

## 📈 系統演進時間軸

```
時間軸：
═══════════════════════════════════════════════════════════

[階段 1] 初始問題發現
├─ 個人資料更新失敗
├─ 來源樣本顯示不需要
└─ AI 辨識率固定 70%

[階段 2] 核心功能修正
├─ ✓ 修正 API 回應檢查邏輯
├─ ✓ 移除來源樣本顯示
└─ ✓ 動態計算 AI 辨識率

[階段 3] 統計邏輯優化
├─ ✓ 統計範圍改為「最近7天」
├─ ✓ 圖表顯示完整星期一到日
└─ ✓ 標題改為「近七天假訊息監測報告」

[階段 4] 自動化系統建立
├─ ✓ 創建每日自動爬蟲腳本
├─ ✓ Windows 工作排程器整合
└─ ✓ 確保每天有新資料

[當前狀態] ✅ 系統穩定運行
├─ 前端顯示正確
├─ 後端邏輯完善
└─ 自動化已設定
```

---

## 🎨 視覺化修改影響範圍

```
修改影響範圍：

        ┌─────────────────────────────┐
        │      用戶介面層 (UI)          │ ← ai_report_page.dart
        │   「近七天假訊息監測報告」     │   (標題、移除來源)
        └─────────────┬───────────────┘
                      │
        ┌─────────────▼───────────────┐
        │      業務邏輯層 (API)         │ ← routes_stats.py
        │   AI辨識率、最近7天統計       │   (核心計算邏輯)
        └─────────────┬───────────────┘
                      │
        ┌─────────────▼───────────────┐
        │      數據存取層 (Data)        │ ← verification_loader.py
        │   載入、分類、分佈計算        │   (資料處理)
        └─────────────┬───────────────┘
                      │
        ┌─────────────▼───────────────┐
        │      數據儲存層 (Storage)     │ ← reports/*.json
        │   JSON 檔案 (22則新聞)        │   (原始資料)
        └─────────────┬───────────────┘
                      │
        ┌─────────────▼───────────────┐
        │    數據生成層 (Generation)    │ ← scraper.py
        │   爬蟲、AI分析、自動化        │   + daily_auto_crawler.py
        └─────────────────────────────┘
```

---

**圖表生成時間:** 2025-10-22  
**系統版本:** Truth Lies Detector v1.0  
**修改範圍:** 前端 UI + 後端 API + 自動化系統
